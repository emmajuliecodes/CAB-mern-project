{
	"name": "server",
	"version": "1.0.0",
	"description": "mern project data",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "concurrently \"nodemon index.js\" \"cd ../client && npm start\""
	},
	"author": "Emma Davidson",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"concurrently": "^8.2.0",
		"cors": "^2.8.5",
		"express": "^4.18.2",
		"nodemon": "^3.0.1"
	}
}
